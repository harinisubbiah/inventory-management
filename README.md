# inventory-management
Browser-based inventory management system — vanilla HTML/CSS/JS dashboard with live charts, sortable/filterable tables, and CRUD logic ported from a C console program. No frameworks, no backend.
